# 版本更新留档 - 错题查询功能

## 更新时间
2026-05-19

## 更新内容
主界面新增错题查询功能，包含测试记录、个人记录、管理记录三个模块。

## 新增文件

### 后端
- `backend/api/record_models.py` — SQLite 错题与测试记录数据模型
- `backend/api/routes/records.py` — 测试记录与错题本 REST API 路由

### 前端
- `frontend/pages/records.html` — 测试记录列表页面（个人记录入口）
- `frontend/pages/mistakes.html` — 个人错题本页面
- `frontend/pages/admin-records.html` — 管理记录页面（全服错题统计，仅管理员）

## 修改文件

### 后端
- `backend/config/settings.py` — 新增 SQLite 数据库路径配置 (`DB_DIR`, `DB_FILE`)
- `backend/api/app.py` — 注册 `records_bp` 蓝图
- `backend/api/models.py` — （未修改，沿用原有用户模型）

### 前端
- `frontend/js/auth.js` — 新增 `saveRecord`、`fetchRecords`、`fetchRecordDetail`、`fetchPersonalMistakes`、`fetchGlobalMistakes`、`goToRecords` 方法
- `frontend/js/exam.js` — 新增 `getWrongAnswers()` 方法，用于提取错题列表
- `frontend/pages/index.html` — 测评结果页面自动调用 `saveExamRecord()` 保存记录（登录状态下）

## 功能说明

1. **登录限制**：测试记录、错题本、管理记录功能必须登录后使用，未登录提示需登录。
2. **自动保存**：用户完成测评后，如果已登录，自动将测试记录（正确率、词汇量、错误单词）保存到后端。
3. **个人记录**：`records.html` 展示每次测试记录（正确率、词汇量、答对/总题数、评级），点击卡片可查看该次测试的错误单词详情。
4. **错题本**：`mistakes.html` 展示个人所有错误单词（去重统计），显示错误次数、首次错误时间、最近错误时间。
5. **管理记录**：`admin-records.html` 仅管理员可访问，SQLite 方式记录全服错误单词及其数量，按错误次数排名展示。

## 数据库表结构

```sql
-- 测试记录表
exam_records (id, user_id, user_name, correct_rate, vocabulary_size, 
              correct_count, total_answered, level, wrong_words, created_at)

-- 个人错题本
personal_mistakes (id, user_id, word, mean, level, mistake_count, 
                   first_time, last_time)

-- 全服错误统计（管理记录）
global_mistakes (id, word, mean, level, mistake_count, 
                 first_time, last_time)
```

## API 接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/api/records` | 保存测试记录 | 需登录 |
| GET  | `/api/records` | 获取个人测试记录列表 | 需登录 |
| GET  | `/api/records/<id>` | 获取单次测试记录详情 | 需登录 |
| GET  | `/api/mistakes/personal` | 获取个人错题本 | 需登录 |
| GET  | `/api/mistakes/global` | 获取全服错题统计 | 仅管理员 |

## 备份文件
所有修改前的原文件均已备份为 `.bak`：
- `frontend/pages/index.html.bak`
- `frontend/js/auth.js.bak`
- `frontend/js/exam.js.bak`
- `backend/api/app.py.bak`
- `backend/api/models.py.bak`
- `backend/config/settings.py.bak`
- `backend/api/routes/auth.py.bak`
